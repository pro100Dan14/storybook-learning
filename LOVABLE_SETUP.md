# 🎯 Настройка Lovable для подключения к бекенду

## Что нужно сделать в Lovable:

### 1. Добавить переменную окружения

В настройках проекта Lovable добавьте:
```
API_BASE_URL=http://162.120.18.86:8787
```

---

### 2. Обновить код для использования API_BASE_URL

Найди все места где используется `fetch('/api/...')` и замени на `fetch(\`${process.env.API_BASE_URL || ''}/api/...\`)`

**Пример:**

Было:
```javascript
const response = await fetch('/api/book', {
  method: 'POST',
  // ...
});
```

Должно быть:
```javascript
const apiBaseUrl = import.meta.env.VITE_API_BASE_URL || '';
const response = await fetch(`${apiBaseUrl}/api/book`, {
  method: 'POST',
  // ...
});
```

**Или для Lovable (проверь как там переменные окружения):**
```javascript
const apiBaseUrl = process.env.API_BASE_URL || '';
const response = await fetch(`${apiBaseUrl}/api/book`, {
  method: 'POST',
  // ...
});
```

---

### 3. Проверить работу

1. Открой DevTools → Network
2. Сделай запрос на генерацию книги
3. Убедись, что запрос идет на `http://162.120.18.86:8787/api/book`

---

## Промпт для Lovable AI:

```
Мне нужно подключить внешний бекенд API к моему приложению.

API URL: http://162.120.18.86:8787

Сделай следующее:
1. Добавь переменную окружения API_BASE_URL=http://162.120.18.86:8787
2. Найди все fetch запросы к /api/* и /jobs/* в коде
3. Замени их на использование переменной API_BASE_URL
4. Убедись, что все API вызовы теперь идут на внешний бекенд вместо локальных путей

API endpoints которые используются:
- GET /api/debug/config
- POST /api/book (body: {name, theme, pages, photoBase64, photoMimeType})
- GET /jobs/:bookId/report.html
- GET /jobs/:bookId/report.json

Проверь что после изменений все запросы идут на http://162.120.18.86:8787 вместо относительных путей.
```





