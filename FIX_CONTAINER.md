# 🔧 Исправление проблем с контейнером

## Диагностика

**На сервере выполните:**

```bash
ssh root@162.120.18.86
cd /opt/storybook-learning

# 1. Проверьте статус контейнера
docker compose ps

# 2. Проверьте логи (последние ошибки)
docker compose logs backend | tail -50

# 3. Попробуйте запустить контейнер
docker compose up -d

# 4. Если не запускается, смотрите ошибки
docker compose logs backend
```

---

## Возможные проблемы

1. Контейнер упал при запуске (ошибка в коде)
2. Порт занят другим процессом
3. Ошибка при сборке


