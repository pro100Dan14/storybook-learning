# 📝 Пошаговая настройка GitHub деплоя

## Шаг 1: Проверка Git

Проверьте, установлен ли Git на вашем компьютере.

### Windows:
1. Откройте PowerShell или CMD
2. Выполните: `git --version`

Если Git не установлен:
- Скачайте: https://git-scm.com/download/win
- Или установите через `winget install Git.Git`

### После установки:
Перезапустите терминал и проверьте: `git --version`

---

## Шаг 2: Инициализация Git репозитория

После того, как Git установлен, выполните в директории проекта:

```bash
git init
git add .
git commit -m "Initial commit"
```

---

## Шаг 3: Создание репозитория на GitHub

1. Зайдите на https://github.com
2. Нажмите **New repository** (или **+** → **New repository**)
3. Название: `storybook-learning` (или любое другое)
4. **Не добавляйте** README, .gitignore, license (у нас уже есть файлы)
5. Нажмите **Create repository**

---

## Шаг 4: Подключение к GitHub

GitHub покажет инструкции. Выполните в PowerShell (замените YOUR_USERNAME):

```bash
git remote add origin https://github.com/YOUR_USERNAME/storybook-learning.git
git branch -M main
git push -u origin main
```

---

## Следующие шаги (после этого)
После выполнения этих шагов, переходим к настройке автоматического деплоя.


