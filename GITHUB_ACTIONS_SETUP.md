# 🚀 Настройка автоматического деплоя через GitHub Actions

## ✅ Текущий статус:
- ✅ Бекенд работает на сервере: http://162.120.18.86:8787
- ✅ Health endpoint отвечает: `{"ok":true}`
- ✅ Контейнер запущен и здоров

---

## Следующий шаг: Настройка SSH ключа для GitHub Actions

GitHub Actions будет автоматически подключаться к вашему серверу и обновлять код при каждом push.

### Шаг 1: Создание SSH ключа для деплоя

**На вашем компьютере (Windows) выполните:**

```bash
# Создаем SSH ключ специально для GitHub Actions
ssh-keygen -t ed25519 -C "github-deploy" -f %USERPROFILE%\.ssh\github_deploy

# Нажмите Enter для всех вопросов (пароль не нужен)
```

**Или в Git Bash:**
```bash
ssh-keygen -t ed25519 -C "github-deploy" -f ~/.ssh/github_deploy
```

---

### Шаг 2: Копирование публичного ключа на сервер

```bash
# Копируем публичный ключ на сервер
type %USERPROFILE%\.ssh\github_deploy.pub | ssh root@162.120.18.86 "cat >> ~/.ssh/authorized_keys"
```

**Или вручную:**
1. Скопируйте содержимое файла `%USERPROFILE%\.ssh\github_deploy.pub`
2. Подключитесь к серверу: `ssh root@162.120.18.86`
3. Выполните: `nano ~/.ssh/authorized_keys`
4. Вставьте ключ в конец файла
5. Сохраните: `Ctrl+X`, `Y`, `Enter`

---

### Шаг 3: Проверка подключения

```bash
ssh -i %USERPROFILE%\.ssh\github_deploy root@162.120.18.86
```

Должно подключиться без пароля.

---

### Шаг 4: Копирование приватного ключа для GitHub

```bash
# Показываем приватный ключ (скопируйте весь вывод)
type %USERPROFILE%\.ssh\github_deploy
```

**Важно:** Скопируйте ВЕСЬ вывод, включая:
- `-----BEGIN OPENSSH PRIVATE KEY-----`
- Весь ключ
- `-----END OPENSSH PRIVATE KEY-----`

---

### Шаг 5: Добавление секретов в GitHub

1. Откройте: https://github.com/pro100Dan14/storybook-learning/settings/secrets/actions
2. Нажмите **New repository secret**
3. Добавьте следующие секреты:

#### `SERVER_HOST`
- **Name:** `SERVER_HOST`
- **Secret:** `162.120.18.86`

#### `SERVER_USER`
- **Name:** `SERVER_USER`
- **Secret:** `root`

#### `SERVER_SSH_KEY`
- **Name:** `SERVER_SSH_KEY`
- **Secret:** (вставьте весь приватный ключ из шага 4)

#### `DEPLOY_PATH` (опционально)
- **Name:** `DEPLOY_PATH`
- **Secret:** `/opt/storybook-learning`

---

### Шаг 6: Проверка автоматического деплоя

После добавления секретов:

1. Сделайте небольшое изменение в коде (например, добавьте комментарий)
2. Закоммитьте и запушьте:

```bash
git add .
git commit -m "Test automatic deployment"
git push
```

3. Откройте: https://github.com/pro100Dan14/storybook-learning/actions
4. Должен запуститься workflow **Deploy to Server**
5. Через 1-2 минуты проверьте на сервере:

```bash
ssh root@162.120.18.86
cd /opt/storybook-learning
docker compose ps
```

---

## ✅ Готово!

Теперь каждый `git push` будет автоматически обновлять бекенд на сервере!

---

## 🐛 Если что-то не работает

### SSH подключение не работает:
- Проверьте, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверьте права: `chmod 600 ~/.ssh/authorized_keys`

### GitHub Actions не может подключиться:
- Проверьте, что все секреты правильно добавлены
- Убедитесь, что приватный ключ скопирован полностью (включая BEGIN/END)

### Деплой не обновляет код:
- Проверьте логи в GitHub Actions
- Проверьте, что на сервере есть `.env` файл (он не должен перезаписываться)





