# ✅ Тестирование автоматического деплоя

## Шаг 1: Тестовый коммит

Сделайте небольшое изменение и закоммитьте:

**На вашем компьютере:**

```bash
# Добавляем обновленный docker-compose.yml (убрали предупреждение)
git add docker-compose.yml
git commit -m "Remove obsolete version from docker-compose.yml"
git push
```

---

## Шаг 2: Проверка GitHub Actions

1. Откройте: https://github.com/pro100Dan14/storybook-learning/actions
2. Должен появиться новый workflow run **"Deploy to Server"**
3. Кликните на него, чтобы увидеть процесс
4. Дождитесь завершения (обычно 1-2 минуты)

**Что должно быть:**
- ✅ Все шаги зелёные
- ✅ Деплой завершился успешно

---

## Шаг 3: Проверка на сервере

После успешного деплоя, проверьте на сервере:

```bash
ssh root@162.120.18.86
cd /opt/storybook-learning
docker compose ps
```

Контейнер должен быть запущен и работать.

---

## Шаг 4: Проверка health endpoint

```bash
curl http://localhost:8787/health
```

Или из браузера:
```
http://162.120.18.86:8787/health
```

---

## ✅ Готово!

Если все работает, теперь каждый `git push` автоматически обновит бекенд на сервере!

---

## 🐛 Если что-то не работает

### GitHub Actions показывает ошибку:
- Проверьте, что все секреты правильно добавлены
- Убедитесь, что SSH ключ правильный
- Посмотрите логи в GitHub Actions - там будет детальная ошибка

### Деплой прошел, но контейнер не работает:
- Проверьте логи: `docker compose logs backend`
- Проверьте, что .env файл существует и правильный

### Не могу подключиться по SSH из GitHub Actions:
- Проверьте, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверьте права: `chmod 600 ~/.ssh/authorized_keys`





