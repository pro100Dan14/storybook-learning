# 📋 Пошаговая инструкция: Настройка сервера

## ✅ Что нужно сделать ПРЯМО СЕЙЧАС:

### 1. Откройте командную строку (PowerShell или CMD) на вашем компьютере

### 2. Подключитесь к серверу:

```bash
ssh root@162.120.18.86
```

**Если спросит подтверждение** - напишите `yes`

**Если попросит пароль** - введите пароль от вашего VPS

---

### 3. После подключения к серверу, скопируйте и вставьте ВСЮ эту команду:

```bash
apt update && apt upgrade -y && apt install -y docker.io docker-compose git curl nano && systemctl start docker && systemctl enable docker && docker --version
```

Это установит все необходимое. Займет 2-5 минут.

---

### 4. Создайте директорию и склонируйте проект:

```bash
mkdir -p /opt/storybook-learning
cd /opt/storybook-learning
git clone https://github.com/pro100Dan14/storybook-learning.git .
```

---

### 5. Создайте .env файл:

```bash
nano .env
```

**Вставьте это** (замените `your_gemini_api_key_here` на ваш реальный ключ):

```
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_TEXT_MODEL=gemini-2.5-flash
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
PROVIDER_TEXT=gemini
PROVIDER_IMAGE=gemini
FACE_ID_ENABLED=false
IDENTITY_SIMILARITY_THRESHOLD=0.62
```

**Сохраните:**
- `Ctrl + X`
- `Y`
- `Enter`

**Защитите файл:**
```bash
chmod 600 .env
```

---

### 6. Запустите проект:

```bash
docker-compose up -d
```

**Проверьте, что все работает:**

```bash
docker-compose ps
curl http://localhost:8787/health
```

Должно вернуть: `{"ok":true,"requestId":"..."}`

---

### 7. Откройте порт (если нужно):

```bash
ufw allow 8787/tcp
```

---

## ✅ Готово!

**Проверьте в браузере:**
http://162.120.18.86:8787/health

---

## 🚀 Следующий шаг

После того, как все работает на сервере, сообщите мне - настроим автоматический деплой через GitHub!


