# 🔧 Восстановление Dockerfile

## Проблема
Dockerfile был удален, но он нужен для сборки контейнера.

## Решение

**На сервере выполните:**

```bash
cd /opt/storybook-learning
git checkout server/Dockerfile
docker compose up -d --build
```

Или создайте файл вручную, если git checkout не сработает.





