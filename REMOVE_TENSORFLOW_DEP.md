# 🔧 Удаление проблемной зависимости

## Проблема
Версия `@tensorflow-models/face-landmarks-detection@^2.1.1` не существует в npm.

## Решение
Удалил пакет из `package.json`, так как:
1. Identity Guard уже отключен (не будет вызывать ошибку)
2. Пакет не обязателен для работы генерации книг
3. Если понадобится позже - можно установить правильную версию

---

## Применить изменения

**Закоммитьте:**
```bash
git add server/package.json
git commit -m "Remove @tensorflow-models/face-landmarks-detection - not needed, identity guard disabled"
git push
```

**На сервере:**
```bash
git pull
docker compose up -d --build
```

Теперь сборка должна пройти успешно!





