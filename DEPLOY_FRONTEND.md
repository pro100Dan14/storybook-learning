# 🌐 Деплой веб-интерфейса на сервер

## Пошаговая инструкция

### Шаг 1: Собрать фронтенд (на вашем компьютере)

```bash
cd web
npm install
npm run build
```

Это создаст папку `web/dist` со собранным фронтендом.

---

### Шаг 2: Закоммитить изменения и запушить

```bash
# Вернуться в корень проекта
cd ..

# Добавить изменения
git add server/index.js web/dist
git commit -m "Add frontend static files serving and build frontend"
git push
```

**Важно:** Добавьте `web/dist` в `.gitignore`, если его там нет, но сначала закоммитьте текущий build.

---

### Шаг 3: Автоматический деплой

GitHub Actions автоматически обновит код на сервере!

Дождитесь завершения деплоя в GitHub Actions.

---

### Шаг 4: Проверка на сервере

После деплоя откройте в браузере:

```
http://162.120.18.86:8787
```

**Должен открыться веб-интерфейс!** 🎉

---

## Альтернатива: Сборка на сервере

Если не хотите коммитить `web/dist`, можно собрать на сервере:

```bash
ssh root@162.120.18.86
cd /opt/storybook-learning/web
npm install
npm run build
cd /opt/storybook-learning
docker compose restart backend
```

---

## После деплоя

Теперь у вас будет:
- **Веб-интерфейс:** http://162.120.18.86:8787
- **API:** http://162.120.18.86:8787/api/*
- **Health:** http://162.120.18.86:8787/health


