# Быстрый старт: Режим сравнения моделей

## Включение режима сравнения

```bash
# В .env файле или через export
export MODEL_COMPARISON_MODE=true
```

## Что происходит

При генерации книги каждая страница будет создана **разной моделью**:

- 📄 Страница 1 → `instantid_artistic`
- 📄 Страница 2 → `instantid`
- 📄 Страница 3 → `instantid_multicontrolnet`
- 📄 Страница 4 → `photomaker_style`

## Результат в API

Каждая страница содержит поле `modelUsed`:

```json
{
  "pages": [
    {
      "pageNumber": 1,
      "modelUsed": "instantid_artistic",
      "similarity": 0.45
    },
    {
      "pageNumber": 2,
      "modelUsed": "instantid",
      "similarity": 0.42
    }
  ]
}
```

## Выбор лучшей модели

1. **Сгенерируйте книгу** через API
2. **Посмотрите результаты** на фронтенде
3. **Выберите лучшую модель** по номеру страницы
4. **Отключите режим сравнения** и установите выбранную модель:

```bash
export MODEL_COMPARISON_MODE=false
export ILLUSTRATION_MODEL=instantid_artistic  # или другая выбранная модель
```

## Пример на фронтенде

```javascript
// После получения ответа от API
const bestModel = pages.find(p => p.pageNumber === selectedPageNumber)?.modelUsed;

// Сохранить выбор
localStorage.setItem('preferredModel', bestModel);

// Использовать для следующих генераций
// (нужно будет отправить запрос на сервер с выбранной моделью)
```

## Отключение режима

```bash
export MODEL_COMPARISON_MODE=false
```

После отключения все страницы будут генерироваться одной моделью (из `ILLUSTRATION_MODEL` или `legacy` по умолчанию).

