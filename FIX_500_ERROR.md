# 🔧 Исправление ошибки 500 и Connection refused

## Проблема
Контейнер не запущен и ошибка 500. Возможно ошибка в коде CORS.

## Диагностика

```bash
ssh root@162.120.18.86
cd /opt/storybook-learning

# 1. Проверьте статус
docker compose ps

# 2. Попробуйте запустить
docker compose up -d

# 3. Если не запускается, посмотрите логи
docker compose logs backend

# 4. Или запустите в foreground чтобы увидеть ошибку
docker compose up
```

Возможно ошибка в синтаксисе JavaScript в CORS конфигурации.

