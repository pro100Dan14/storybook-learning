# 🔧 Исправление ошибки "src refspec main does not match any"

## Проблема
Ошибка возникает потому, что в локальном репозитории нет коммитов. Ветка `main` создается только после первого коммита.

## Решение

### 1. Проверьте, что файлы добавлены
```bash
git status
```

Вы должны увидеть список файлов под заголовком "Untracked files" или "Changes to be committed"

### 2. Добавьте все файлы
```bash
git add .
```

### 3. Сделайте первый коммит
```bash
git commit -m "Initial commit"
```

**Если появится ошибка про email/username**, настройте их:
```bash
git config --global user.name "Ваше имя"
git config --global user.email "ваш-email@example.com"
```

Затем снова:
```bash
git commit -m "Initial commit"
```

### 4. Теперь можете пушить
```bash
git push -u origin main
```

---

## Если remote origin уже существует (ошибка "remote origin already exists")

Вы уже добавили remote, это нормально. Просто продолжайте со шага 2 выше.

---

## Проверка после успешного push

Зайдите на GitHub: https://github.com/pro100Dan14/storybook-learning

Вы должны увидеть все ваши файлы в репозитории!








