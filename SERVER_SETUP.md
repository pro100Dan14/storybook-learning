# 🖥️ Настройка сервера для деплоя

## Данные вашего сервера:
- **IP:** 162.120.18.86
- **Пользователь:** root
- **Система:** Ubuntu

---

## Шаг 1: Подключение к серверу

### На вашем компьютере (Windows) выполните:

```bash
ssh root@162.120.18.86
```

Если это первый раз, система спросит подтверждение - напишите `yes` и нажмите Enter.

**Если нужно ввести пароль** - введите пароль от сервера.

---

## Шаг 2: Обновление системы и установка зависимостей

### После подключения к серверу выполните:

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем необходимые пакеты
apt install -y docker.io docker-compose git curl nano

# Запускаем Docker
systemctl start docker
systemctl enable docker

# Проверяем, что Docker работает
docker --version
docker-compose --version
```

---

## Шаг 3: Создание директории проекта

```bash
# Создаем директорию для проекта
mkdir -p /opt/storybook-learning
cd /opt/storybook-learning
```

---

## Шаг 4: Клонирование репозитория

```bash
# Клонируем ваш репозиторий
git clone https://github.com/pro100Dan14/storybook-learning.git .

# Проверяем, что файлы скопировались
ls -la
```

---

## Шаг 5: Создание .env файла

```bash
# Создаем .env файл
nano .env
```

### Вставьте в файл (замените `your_comfy_cloud_api_key_here` на ваш ключ):

```
COMFY_CLOUD_API_KEY=your_comfy_cloud_api_key_here
COMFY_CLOUD_BASE_URL=https://cloud.comfy.org

# Optional (если используете старые Gemini endpoints)
# GEMINI_API_KEY=your_gemini_api_key_here
```

**Сохранение в nano:**
- Нажмите `Ctrl + X`
- Затем `Y` (да)
- Затем `Enter`

**Установите правильные права:**
```bash
chmod 600 .env
```

---

## Шаг 6: Первый запуск (проверка)

```bash
# Запускаем Docker контейнер
docker-compose up -d

# Ждем несколько секунд
sleep 5

# Проверяем статус
docker-compose ps

# Проверяем логи
docker-compose logs backend

# Проверяем health endpoint
curl http://localhost:8787/health
```

Если все работает, увидите: `{"ok":true,"requestId":"..."}`

---

## Шаг 7: Проверка доступа извне

**Важно:** Убедитесь, что порт 8787 открыт в firewall:

```bash
# Если используется ufw
ufw allow 8787/tcp
ufw status

# Или проверьте, что порт слушается
netstat -tuln | grep 8787
```

---

## Готово! ✅

Теперь ваш бекенд работает на сервере!

**Проверьте из браузера:**
- http://162.120.18.86:8787/health

---

## Следующий шаг

После настройки сервера переходим к настройке **автоматического деплоя через GitHub Actions**.








