# 🚀 Настройка автоматического деплоя на GitHub

Это руководство поможет вам настроить автоматический деплой бекенда на ваш сервер через GitHub Actions.

## 📋 Что нужно подготовить

### 1. Сервер с доступом по SSH

Вам нужен сервер со следующими требованиями:
- **Linux** (Ubuntu, Debian, CentOS и т.д.)
- **SSH доступ** с правами sudo (или root)
- **Docker и Docker Compose** (рекомендуется) или **Node.js + PM2**
- **Git** (для обновления кода)

### 2. Настройка SSH ключа

#### На вашем компьютере (локально):

1. Создайте SSH ключ специально для деплоя (если еще нет):
```bash
ssh-keygen -t ed25519 -C "github-deploy" -f ~/.ssh/github_deploy
```

2. Скопируйте публичный ключ на сервер:
```bash
ssh-copy-id -i ~/.ssh/github_deploy.pub user@your-server.com
```

3. Проверьте подключение:
```bash
ssh -i ~/.ssh/github_deploy user@your-server.com
```

4. Скопируйте **приватный ключ** (нужен для GitHub Secrets):
```bash
cat ~/.ssh/github_deploy
```
Скопируйте весь вывод (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

### 3. Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

#### Обязательные секреты:

| Название | Описание | Пример |
|----------|----------|--------|
| `SERVER_HOST` | IP адрес или доменное имя сервера | `192.168.1.100` или `myserver.com` |
| `SERVER_USER` | Пользователь для SSH подключения | `deploy` или `root` |
| `SERVER_SSH_KEY` | Приватный SSH ключ (весь ключ, включая BEGIN/END) | `-----BEGIN OPENSSH PRIVATE KEY-----...` |

#### Опциональные секреты:

| Название | Описание | По умолчанию |
|----------|----------|--------------|
| `SERVER_PORT` | SSH порт | `22` |
| `DEPLOY_PATH` | Путь на сервере для проекта | `/opt/storybook-learning` |

### 4. Первоначальная настройка сервера

Подключитесь к серверу и выполните:

```bash
# Установите Docker и Docker Compose (если еще не установлены)
# Для Ubuntu/Debian:
sudo apt update
sudo apt install -y docker.io docker-compose git

# Создайте директорию для проекта
sudo mkdir -p /opt/storybook-learning
sudo chown $USER:$USER /opt/storybook-learning
cd /opt/storybook-learning

# Клонируйте репозиторий (первый раз)
git clone https://github.com/ВАШ-USERNAME/storybook-learning.git .

# Создайте .env файл с вашими настройками
cat > .env << 'EOF'
GEMINI_API_KEY=ваш_api_ключ_здесь
GEMINI_TEXT_MODEL=gemini-2.5-flash
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image
PROVIDER_TEXT=gemini
PROVIDER_IMAGE=gemini
FACE_ID_ENABLED=false
IDENTITY_SIMILARITY_THRESHOLD=0.62
EOF

# Установите права на .env (чтобы только вы могли читать)
chmod 600 .env
```

**Важно:** Файл `.env` должен уже существовать на сервере до первого деплоя, так как workflow не будет его перезаписывать.

## 🎯 Как это работает

### Автоматический деплой

После настройки, каждый раз когда вы делаете `git push` в ветку `main` или `master`:

1. GitHub Actions автоматически запускает workflow
2. Workflow подключается к вашему серверу по SSH
3. Обновляет код из репозитория (`git pull`)
4. Пересобирает Docker контейнер (или перезапускает Node.js приложение)
5. Проверяет работоспособность через health check

### Ручной запуск деплоя

Вы также можете запустить деплой вручную:
1. Откройте вкладку **Actions** в вашем репозитории
2. Выберите workflow **Deploy to Server**
3. Нажмите **Run workflow**
4. Выберите ветку и нажмите **Run workflow**

## 🔧 Варианты деплоя

### Вариант 1: Docker (рекомендуется)

Если на сервере установлен Docker и Docker Compose, workflow автоматически использует Docker:

- Запускается через `docker-compose up -d`
- Автоматические перезапуски при сбоях
- Изоляция окружения
- Легкое управление версиями

### Вариант 2: Node.js + PM2

Если Docker не используется, можно использовать PM2:

```bash
# На сервере установите PM2
npm install -g pm2

# Workflow автоматически перезапустит приложение через PM2
```

## 📝 Проверка деплоя

После деплоя проверьте:

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Проверьте статус контейнера (Docker)
docker-compose ps

# Или статус через PM2
pm2 status

# Проверьте логи
docker-compose logs -f backend
# или
pm2 logs storybook-backend

# Проверьте health endpoint
curl http://localhost:8787/health
```

## 🐛 Troubleshooting

### Ошибка SSH подключения

**Проблема:** `Permission denied (publickey)`

**Решение:**
1. Убедитесь, что публичный ключ добавлен на сервер: `~/.ssh/authorized_keys`
2. Проверьте права: `chmod 600 ~/.ssh/authorized_keys`
3. Убедитесь, что в GitHub Secret добавлен правильный приватный ключ

### Ошибка Docker

**Проблема:** `docker-compose: command not found`

**Решение:**
```bash
# Установите Docker Compose на сервере
sudo apt install docker-compose
# или используйте docker compose (новая версия)
```

### Ошибка .env файла

**Проблема:** Переменные окружения не работают

**Решение:**
- Убедитесь, что файл `.env` существует на сервере в `/opt/storybook-learning`
- Проверьте права: `chmod 600 .env`
- Проверьте формат файла (без пробелов вокруг `=`)

### Health check failed

**Проблема:** Деплой завершается, но health check не проходит

**Решение:**
```bash
# Проверьте логи на сервере
docker-compose logs backend

# Проверьте, что порт 8787 открыт
sudo netstat -tuln | grep 8787

# Проверьте, что приложение запущено
docker-compose ps
```

## 🔐 Безопасность

1. **Никогда не коммитьте** файлы с секретами (`.env`, `gcp-sa.json`)
2. Используйте GitHub Secrets для хранения чувствительных данных
3. Ограничьте доступ к SSH ключам (права `600`)
4. Используйте отдельного пользователя для деплоя (не root)
5. Регулярно обновляйте сервер и зависимости

## 📚 Дополнительные материалы

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Docker Compose документация](https://docs.docker.com/compose/)
- [PM2 документация](https://pm2.keymetrics.io/docs/usage/quick-start/)

## 🆘 Помощь

Если что-то не работает:
1. Проверьте логи в GitHub Actions (вкладка Actions → выберите workflow run)
2. Проверьте логи на сервере (`docker-compose logs` или `pm2 logs`)
3. Убедитесь, что все секреты правильно настроены
4. Проверьте, что сервер доступен из интернета (SSH порт открыт)

---

**Готово!** Теперь каждый push в main будет автоматически деплоить ваше приложение на сервер! 🎉

