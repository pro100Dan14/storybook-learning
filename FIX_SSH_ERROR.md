# 🔧 Исправление ошибки "Permission denied (publickey)"

## Проблема
Вы пытаетесь подключиться через SSH (`git@github.com`), но SSH ключ не настроен на GitHub.

## Решение (вариант 1): Использовать HTTPS вместо SSH (проще!)

### Измените remote URL на HTTPS:

```bash
git remote set-url origin https://github.com/pro100Dan14/storybook-learning.git
```

### Теперь попробуйте push:

```bash
git push -u origin main
```

GitHub попросит логин и пароль. Но с 2021 года обычный пароль не работает.

### Нужен Personal Access Token (PAT):

1. Зайдите на GitHub: https://github.com/settings/tokens
2. Нажмите **Generate new token** → **Generate new token (classic)**
3. Название: `storybook-deploy`
4. Выберите срок действия (например, 90 дней или без ограничений)
5. Выберите права: **repo** (все галочки)
6. Нажмите **Generate token**
7. **СКОПИРУЙТЕ ТОКЕН СРАЗУ!** Он показывается только один раз!

### Использование токена:

При push GitHub попросит:
- **Username**: `pro100Dan14`
- **Password**: вставьте **токен** (не пароль!)

---

## Решение (вариант 2): Настроить SSH ключ

Если хотите использовать SSH:

1. Создайте SSH ключ:
```bash
ssh-keygen -t ed25519 -C "your-email@example.com"
```
Нажмите Enter для всех вопросов.

2. Скопируйте публичный ключ:
```bash
type %USERPROFILE%\.ssh\id_ed25519.pub
```
(Или `cat ~/.ssh/id_ed25519.pub` в Git Bash)

3. На GitHub: Settings → SSH and GPG keys → New SSH key
4. Вставьте ключ и сохраните
5. Проверьте:
```bash
ssh -T git@github.com
```

---

## Рекомендация

**Для начала используйте вариант 1 (HTTPS)** - это быстрее и проще!


