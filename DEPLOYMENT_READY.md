# 🎉 Автоматический деплой настроен!

## ✅ Что мы сделали:
1. ✅ Настроили сервер (Docker, проект запущен)
2. ✅ Создали SSH ключ для деплоя
3. ✅ Добавили секреты в GitHub
4. ✅ Настроили GitHub Actions workflow

---

## 🚀 Тестирование

### Шаг 1: Сделайте тестовый коммит

**На вашем компьютере:**

```bash
# Добавляем обновленные файлы
git add docker-compose.yml server/Dockerfile
git commit -m "Setup automatic deployment via GitHub Actions"
git push
```

---

### Шаг 2: Проверьте GitHub Actions

1. Откройте: **https://github.com/pro100Dan14/storybook-learning/actions**

2. Должен появиться workflow run **"Deploy to Server"**

3. Кликните на него и следите за процессом

4. Дождитесь завершения (1-2 минуты)

**Должно быть:**
- ✅ Зеленые галочки на всех шагах
- ✅ Сообщение "Деплой успешно завершен!"

---

### Шаг 3: Проверка на сервере

После успешного деплоя:

```bash
ssh root@162.120.18.86
cd /opt/storybook-learning
docker compose ps
curl http://localhost:8787/health
```

---

## ✅ Готово!

Теперь каждый раз, когда вы делаете `git push`, бекенд автоматически обновится на сервере!

---

## 📝 Что дальше?

### Ручной запуск деплоя:
Вы можете запустить деплой вручную через GitHub UI:
1. Откройте: **Actions** → **Deploy to Server**
2. Нажмите **Run workflow**
3. Выберите ветку и нажмите **Run workflow**

### Просмотр логов деплоя:
Все логи деплоя доступны в разделе **Actions** на GitHub

### Добавление FaceID (потом):
Когда понадобится FaceID, обновите Dockerfile, добавив Python зависимости обратно.

---

## 🆘 Помощь

Если что-то не работает, проверьте:
- Логи в GitHub Actions (там будет детальная ошибка)
- Статус контейнера на сервере: `docker compose ps`
- Логи бекенда: `docker compose logs backend`








